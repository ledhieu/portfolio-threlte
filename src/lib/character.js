import { writable } from "svelte/store";

export const hipHop = writable(false);
export const breakDance = writable(false);
export const idle = writable(false);
export const darkMode = writable(false)
